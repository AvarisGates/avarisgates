package com.avaris.avarisgates.core.api.config;

import com.avaris.avarisgates.ModConfig;

/**
 * And interface defining the functionality of a Mod Config.
 * Use in{@link AbstractConfigManager#AbstractConfigManager(Class)}to assign which class should be read from.<br>
 *
 * Also see{@link ModConfig} for an implementation
 */
public interface IModConfig {
    // Another dummy function to initialize static members
    static void init() {
    }
}
